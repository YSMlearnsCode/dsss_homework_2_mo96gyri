from setuptools import setup, find_packages

setup(
    name="dsss_homework_2",
    version="0.1",
    packages=find_packages(),  # This will find the math_quiz package
    install_requires=[],
    description="A package for DSSS Homework 2",
)
